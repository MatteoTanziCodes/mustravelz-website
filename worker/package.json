{
	"name": "@mustravelz/worker",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"deploy:development": "wrangler deploy --env development",
		"d1:migrate:local": "wrangler d1 migrations apply mustravelz-development --local --env development",
		"d1:migrate:development": "wrangler d1 migrations apply mustravelz-development --remote --env development",
		"d1:migrate:production": "wrangler d1 migrations apply mustravelz-production --remote",
		"cf-typegen": "wrangler types ./src/types/cloudflare-env.d.ts"
	},
	"devDependencies": {
		"typescript": "^5.7.4",
		"wrangler": "^4.80.0"
	}
}
