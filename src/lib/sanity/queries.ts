export const homePageQuery = `
	*[_type == "homePage" && locale == $locale][0]{
		locale,
		seoTitle,
		seoDescription,
		announcement,
		nav[]{
			label,
			href
		},
		hero{
			eyebrow,
			title,
			summary,
			primaryCta{
				label,
				href
			},
			secondaryCta{
				label,
				href
			},
			stats[]{
				label,
				value
			}
		},
		notebookCards[]{
			kicker,
			title,
			description
		},
		collections{
			eyebrow,
			title,
			description,
			cards[]{
				eyebrow,
				title,
				description
			}
		},
		journal{
			eyebrow,
			title,
			description,
			stories[]{
				access,
				title,
				description
			}
		},
		community{
			eyebrow,
			title,
			description,
			approvalNote,
			steps[]{
				title,
				description
			},
			cta{
				label,
				href
			}
		},
		footerNote
	}
`;
